# Copy to .env and fill in. The connection string comes from the Neon project
# (Neon console → your project → Connect). Server-side only; do not prefix with VITE_.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require
